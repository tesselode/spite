[package]
name = "spite"
version = "0.1.0"
edition = "2024"

[dependencies]

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.62.2", features = [
	"Gaming_Input",
	"Gaming_Input_ForceFeedback",
] }
